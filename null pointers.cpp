# include <stdio.h>
int main()
{
int *ptr = NULL;
printf("\n the value of ptr %x", ptr);
printf ("\n the address of ptr is % x", &ptr);
printf("\n the ptr pointing value %d", *ptr);
return 0;
}
